import '../../index.css';
function Landing() {
    return (   //add if statement checking if the auth user is logged in and if so return the dashboard view instead
    <h1 className="text-3xl font-bold underline">
    Landing Page
  </h1>
  );
}

export default Landing;